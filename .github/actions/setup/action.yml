name: "Setup project"
description: "Install uv and sync dependencies"
runs:
  using: "composite"
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v8.2.0
    - name: Install dependencies
      run: uv sync --dev
      shell: bash
